From: L. David Baron <dbaron@dbaron.org>

Bug 561674:  add --with-debug-username=USER configure option.

diff --git a/configure.in b/configure.in
--- a/configure.in
+++ b/configure.in
@@ -6930,16 +6930,20 @@ MOZ_ARG_ENABLE_STRING(debug,
         _MOZ_DEBUG_FLAGS_SET=1
     fi
   else
     MOZ_DEBUG=
   fi ],
   MOZ_DEBUG=)
 
 MOZ_DEBUG_ENABLE_DEFS="-DDEBUG -D_DEBUG -DTRACING"
+MOZ_ARG_WITH_STRING(debug-username,
+[  --with-debug-username=USER
+                          Username for DEBUG_user macros.],
+    MOZ_DEBUG_ENABLE_DEFS="$MOZ_DEBUG_ENABLE_DEFS -DDEBUG_$withval")
 
 MOZ_DEBUG_DISABLE_DEFS="-DNDEBUG -DTRIMMED"
 
 if test -n "$MOZ_DEBUG"; then
     AC_MSG_CHECKING([for valid debug flags])
     _SAVE_CFLAGS=$CFLAGS
     CFLAGS="$CFLAGS $MOZ_DEBUG_FLAGS"
     AC_TRY_COMPILE([#include <stdio.h>], 
